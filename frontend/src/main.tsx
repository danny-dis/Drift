import React from "react";
import ReactDOM from "react-dom/client";
import DriftApp from "./DriftApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode><DriftApp /></React.StrictMode>
);
